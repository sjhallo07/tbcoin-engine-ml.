# requirements-ml.txt
#
# This extras file layers common machine-learning dependencies on top of the
# core backend requirements. All versions are pinned for reproducibility on CPU
# environments. Update in tandem with the main requirements when bumping ML
# stacks. For GPU usage, follow the official installation guides for the
# respective frameworks and install the correct CUDA-enabled wheels.

# Inherit base dependencies (FastAPI, database drivers, etc.)
-r requirements.txt

# Deep learning toolkits
torch==2.5.1
torchvision==0.20.1
tensorflow==2.15.0

# General machine-learning ecosystem
transformers==4.36.2
scikit-learn==1.8.0
xgboost==2.0.1
lightgbm==4.1.0
stable-baselines3==2.0.0

# Experiment tracking & utilities
mlflow==2.10.1
pandas==2.1.3
numpy==1.25.2
scipy==1.16.3
joblib==1.5.2
PyYAML==6.0.3